import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    backgroundColor: "#E5BF3C",
    width: "100%",
    color: "black",
    textAlign: "center",
    fontSize: 20,
    fontWeight: "bold",
    height: 40,
    // borderWidth: 2,
    // borderColor: "white",
    borderRadius: 5,
    padding: 10,
  },
});
